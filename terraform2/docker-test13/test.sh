docker-compose exec terraform terraform init
docker-compose exec terraform terraform apply
docker-compose run -it terraform validate
docker-compose run -it terraform init
docker-compose run -it terraform plan
docker-compose run -it terraform providers
docker-compose run -it terraform apply
